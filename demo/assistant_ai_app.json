{
  "app_name": "ASSISTANT_AI App",
  "tag": "#assistant_ai",
  "export_date": "2026-07-09T11:57:35.508Z",
  "components": {
    "entities": [
      {
        "id": 12,
        "name": "Yapay Zeka Günlük Raporları",
        "slug": "ai_reports",
        "schema": "{\"date\":{\"type\":\"string\",\"label\":\"Tarih (YYYY-MM-DD)\",\"rules\":{\"required\":true},\"isPrimary\":false,\"showInTable\":true,\"_order\":0},\"report_text\":{\"type\":\"string\",\"label\":\"Sentezlenmiş Brifing Metni\",\"rules\":{\"required\":true},\"isPrimary\":false,\"showInTable\":true,\"_order\":1},\"is_read\":{\"type\":\"boolean\",\"default\":false,\"label\":\"Kullanıcı Tarafından Okundu mu?\",\"rules\":{\"required\":false},\"isPrimary\":false,\"showInTable\":true,\"_order\":2}}",
        "created_at": "2026-07-03 20:50:40",
        "updated_at": "2026-07-07 07:55:54",
        "created_by": null,
        "updated_by": null,
        "hashtags": "[\"#assistant_ai\"]"
      },
      {
        "id": 13,
        "name": "Yapay Zeka Ayarları",
        "slug": "ai_settings",
        "schema": "{\"ai_provider\":{\"type\":\"string\",\"default\":\"gemini\",\"label\":\"Yapay Zeka Sağlayıcısı (gemini veya openai)\",\"rules\":{\"required\":true},\"isPrimary\":false,\"showInTable\":true,\"_order\":0},\"api_key\":{\"type\":\"password\",\"label\":\"API Anahtarı\",\"rules\":{\"required\":true},\"isPrimary\":false,\"showInTable\":true,\"_order\":1},\"system_prompt\":{\"type\":\"string\",\"default\":\"Sen enerjik, motive edici ve esprili bir kişisel asistansın. Sana verilen hava durumu, haberler ve kripto piyasası verilerini kullanarak bana akıcı, 1 dakikada okunabilecek Türkçe bir sabah brifingi hazırla.\",\"label\":\"Asistan Kişiliği (System Prompt)\",\"rules\":{\"required\":false},\"isPrimary\":false,\"showInTable\":true,\"_order\":2}}",
        "created_at": "2026-07-03 20:50:47",
        "updated_at": "2026-07-07 07:55:54",
        "created_by": null,
        "updated_by": null,
        "hashtags": "[\"#assistant_ai\"]"
      }
    ],
    "system_variables": [],
    "dynamic_utils": [],
    "records": [],
    "schedulers": [],
    "roles": [],
    "languages": [],
    "translations": [],
    "pages": [
      {
        "id": 35,
        "route_pattern": "ai-avatar",
        "title": "Konuşan AI Asistan",
        "page_type": "regular",
        "template_string": "<v-container fluid class=\"bg-grey-lighten-4 fill-height pa-4 d-flex flex-column align-center justify-center\" style=\"background: radial-gradient(circle at center, #1a1a2e 0%, #16213e 100%);\">\n  \n  <style>\n    .orb-container {\n      display: flex; justify-content: center; align-items: center; height: 350px;\n    }\n    .ai-orb {\n      width: 180px; height: 180px;\n      border-radius: 50%;\n      background: radial-gradient(circle at 30% 30%, #00d2ff 0%, #3a7bd5 100%);\n      box-shadow: 0 0 50px #00d2ff, inset 0 0 40px rgba(255,255,255,0.6);\n      animation: breathe 4s ease-in-out infinite;\n      cursor: pointer;\n      transition: transform 0.3s;\n    }\n    .ai-orb.speaking {\n      animation: speak 0.6s infinite alternate;\n    }\n    @keyframes breathe {\n      0%, 100% { transform: scale(1); box-shadow: 0 0 50px #00d2ff; }\n      50% { transform: scale(1.08); box-shadow: 0 0 80px #00d2ff, 0 0 20px #fff; }\n    }\n    @keyframes speak {\n      0% { transform: scale(1) scaleY(1); box-shadow: 0 0 50px #00d2ff; }\n      100% { transform: scale(1.15) scaleY(0.95); box-shadow: 0 0 100px #00d2ff, 0 0 40px #fff; }\n    }\n  </style>\n\n  <!-- Üst Araç Çubuğu -->\n  <div style=\"position: absolute; top: 16px; right: 16px;\">\n    <v-btn icon=\"mdi-cog\" variant=\"tonal\" color=\"white\" @click=\"showSettings = true\" title=\"Asistan Ayarları\"></v-btn>\n  </div>\n\n  <!-- 3D AI Orb -->\n  <div class=\"orb-container\" @click=\"toggleSpeech\">\n    <div class=\"ai-orb\" :class=\"{ speaking: isSpeaking }\"></div>\n  </div>\n\n  <!-- Durum Mesajı -->\n  <div class=\"text-center mt-6\" style=\"max-width: 600px;\">\n    <h2 class=\"text-h4 font-weight-bold text-white mb-2\">Dijital Asistanınız</h2>\n    \n    <div v-if=\"loadingReport\" class=\"d-flex align-center justify-center text-white\">\n      <v-progress-circular indeterminate size=\"24\" class=\"mr-2\" color=\"cyan\"></v-progress-circular>\n      Brifing Hazırlanıyor, lütfen bekleyin...\n    </div>\n    \n    <div v-else-if=\"!todayReport\" class=\"text-grey-lighten-1\">\n      <p>Bugün için hazırlanmış bir sabah brifingi bulunmuyor.</p>\n      <v-btn color=\"cyan\" variant=\"flat\" class=\"mt-4 rounded-pill font-weight-bold\" @click=\"generateReport\">\n        <v-icon icon=\"mdi-brain\" class=\"mr-2\"></v-icon> Günü Başlat (Brifing Üret)\n      </v-btn>\n    </div>\n\n    <div v-else>\n      <v-card color=\"rgba(255,255,255,0.1)\" class=\"rounded-xl text-white pa-4 elevation-0\" style=\"backdrop-filter: blur(10px);\">\n        <div class=\"d-flex justify-space-between align-center mb-4\">\n          <div class=\"text-subtitle-1 font-weight-bold text-cyan\">{{ todayReport.date }} Raporu</div>\n          <v-chip size=\"small\" :color=\"todayReport.is_read ? 'success' : 'warning'\">{{ todayReport.is_read ? 'Okundu' : 'Yeni' }}</v-chip>\n        </div>\n        <div class=\"text-body-1 text-left\" style=\"max-height: 200px; overflow-y: auto; white-space: pre-wrap; font-size: 1.1rem; line-height: 1.6;\">\n          {{ todayReport.report_text }}\n        </div>\n        <v-btn color=\"cyan\" variant=\"flat\" class=\"mt-4 w-100 rounded-pill font-weight-bold\" @click=\"toggleSpeech\">\n          <v-icon :icon=\"isSpeaking ? 'mdi-stop' : 'mdi-play'\" class=\"mr-2\"></v-icon> {{ isSpeaking ? 'Durdur' : 'Sesli Oku' }}\n        </v-btn>\n      </v-card>\n    </div>\n  </div>\n\n  <!-- Settings Dialog -->\n  <v-dialog v-model=\"showSettings\" max-width=\"500\">\n    <v-card class=\"rounded-xl\">\n      <v-card-title class=\"bg-primary text-white\">Asistan Ayarları</v-card-title>\n      <v-card-text class=\"pt-4\">\n        <p class=\"text-body-2 mb-4 text-grey-darken-2\">Asistanın rapor üretebilmesi için ücretsiz bir Gemini API Anahtarı veya OpenAI API Anahtarı girmelisiniz.</p>\n        <v-select\n          v-model=\"aiProvider\"\n          :items=\"[{title: 'Google Gemini', value: 'gemini'}, {title: 'OpenAI (ChatGPT)', value: 'openai'}]\"\n          label=\"Yapay Zeka Servisi\"\n          variant=\"outlined\"\n        ></v-select>\n        <v-text-field\n          v-model=\"aiKey\"\n          label=\"API Anahtarı (API Key)\"\n          variant=\"outlined\"\n          type=\"password\"\n        ></v-text-field>\n      </v-card-text>\n      <v-card-actions class=\"pa-4 pt-0\">\n        <v-spacer></v-spacer>\n        <v-btn color=\"grey\" variant=\"text\" @click=\"showSettings = false\">İptal</v-btn>\n        <v-btn color=\"primary\" variant=\"flat\" @click=\"saveSettings\">Kaydet</v-btn>\n      </v-card-actions>\n    </v-card>\n  </v-dialog>\n\n</v-container>",
        "script_content": "}; return {\n  setup() {\n    const aiKey = ref('');\n    const aiProvider = ref('gemini');\n    const showSettings = ref(false);\n    \n    const todayReport = ref(null);\n    const isSpeaking = ref(false);\n    const loadingReport = ref(false);\n    \n    let synth = window.speechSynthesis;\n    let utterance = null;\n\n    const loadSettings = async () => {\n       try {\n           const res = await $fetch('/api/admin/records/ai_settings?limit=1');\n           const recs = res.records || [];\n           if(recs.length > 0) {\n              aiKey.value = recs[0].api_key || '';\n              aiProvider.value = recs[0].ai_provider || 'gemini';\n           } else {\n              showSettings.value = true;\n           }\n       } catch(e) {}\n    };\n\n    const saveSettings = async () => {\n       try {\n           const res = await $fetch('/api/admin/records/ai_settings?limit=1');\n           const recs = res.records || [];\n           if(recs.length > 0) {\n              await $fetch(`/api/admin/records/ai_settings/${recs[0].id}`, {\n                 method: 'PUT',\n                 body: { data: { api_key: aiKey.value, ai_provider: aiProvider.value } }\n              });\n           } else {\n              await $fetch('/api/admin/records/ai_settings', {\n                 method: 'POST',\n                 body: { data: { api_key: aiKey.value, ai_provider: aiProvider.value, system_prompt: \"Sen profesyonel, net, kısa ve öz konuşan bir dijital asistansın. KESİNLİKLE EMOJI KULLANMA. KESİNLİKLE MARKDOWN (*, **, # vb) KULLANMA. Düz metin olarak yanıt ver. Gereksiz uzatma, laf salatası yapma. Doğrudan bilgileri ver ve bitir.\" } }\n              });\n           }\n           showSettings.value = false;\n       } catch(e) { alert(\"Hata: \" + e.message); }\n    };\n\n    const loadTodayReport = async () => {\n       loadingReport.value = true;\n       const today = new Date().toISOString().split('T')[0];\n       try {\n           const res = await $fetch(`/api/admin/records/ai_reports?limit=1&sort=-id`);\n           const recs = res.records || [];\n           if(recs.length > 0 && recs[0].date === today) {\n              todayReport.value = recs[0];\n           } else {\n              todayReport.value = null;\n           }\n       } catch(e) {}\n       loadingReport.value = false;\n    };\n\n    const generateReport = async () => {\n       if(!aiKey.value) {\n           showSettings.value = true;\n           return;\n       }\n       loadingReport.value = true;\n       try {\n           const res = await $fetch('/api/custom/ai-brain');\n           if(res.success) {\n              await loadTodayReport();\n              if(todayReport.value) toggleSpeech();\n           } else {\n              alert(\"Yapay Zeka Hatası: \" + res.message);\n           }\n       } catch(e) { alert(\"Hata: \" + e.message); }\n       loadingReport.value = false;\n    };\n\n    const cleanTextForSpeech = (text) => {\n        // Emoji ve markdown temizliği\n        return text.replace(/[*_#]/g, '').replace(/[\\u{1F600}-\\u{1F64F}\\u{1F300}-\\u{1F5FF}\\u{1F680}-\\u{1F6FF}\\u{1F700}-\\u{1F77F}\\u{1F780}-\\u{1F7FF}\\u{1F800}-\\u{1F8FF}\\u{1F900}-\\u{1F9FF}\\u{1FA00}-\\u{1FA6F}\\u{1FA70}-\\u{1FAFF}\\u{2600}-\\u{26FF}\\u{2700}-\\u{27BF}]/gu, '').trim();\n    };\n\n    const toggleSpeech = () => {\n       if(!todayReport.value) return;\n       \n       if(synth.speaking || isSpeaking.value) {\n          synth.cancel();\n          isSpeaking.value = false;\n          return;\n       }\n       \n       const textToRead = cleanTextForSpeech(todayReport.value.report_text);\n       utterance = new SpeechSynthesisUtterance(textToRead);\n       utterance.lang = 'tr-TR';\n       \n       utterance.onstart = () => isSpeaking.value = true;\n       utterance.onend = () => {\n           isSpeaking.value = false;\n           if(!todayReport.value.is_read) {\n              try {\n                  $fetch(`/api/admin/records/ai_reports/${todayReport.value.id}`, {\n                     method: 'PUT',\n                     body: { data: { is_read: true, date: todayReport.value.date, report_text: todayReport.value.report_text } }\n                  });\n                  todayReport.value.is_read = true;\n              } catch(e){}\n           }\n       };\n       \n       const voices = synth.getVoices();\n       let trVoice = voices.find(v => v.lang.includes('tr') && v.name.toLowerCase().includes('natural'));\n       if (!trVoice) trVoice = voices.find(v => v.lang.includes('tr') && v.name.includes('Emel'));\n       if (!trVoice) trVoice = voices.find(v => v.lang.includes('tr') && v.name.includes('Tolga'));\n       if (!trVoice) trVoice = voices.find(v => v.lang.includes('tr') && v.name.includes('Microsoft'));\n       if (!trVoice) trVoice = voices.find(v => v.lang.includes('tr') && v.name.includes('Google'));\n       if (!trVoice) trVoice = voices.find(v => v.lang.includes('tr'));\n       \n       if(trVoice) utterance.voice = trVoice;\n       synth.speak(utterance);\n    };\n\n    onMounted(async () => {\n       await loadSettings();\n       if(!showSettings.value) await loadTodayReport();\n       \n       if (speechSynthesis.onvoiceschanged !== undefined) {\n          speechSynthesis.onvoiceschanged = () => {};\n       }\n    });\n\n    onUnmounted(() => {\n       if(synth.speaking) synth.cancel();\n    });\n\n    return { \n      aiKey, aiProvider, showSettings,\n      todayReport, isSpeaking, loadingReport,\n      saveSettings, generateReport, toggleSpeech\n    };\n  }\n}; function __dummy() {",
        "style_content": "",
        "active": 1,
        "is_public": 1,
        "is_landing_page": 0,
        "created_at": "2026-07-03 20:51:02",
        "updated_at": "2026-07-03 21:40:27",
        "created_by": 1,
        "updated_by": 1,
        "hashtags": "[\"#assistant_ai\"]",
        "priority": 0
      }
    ],
    "endpoints": [
      {
        "id": 31,
        "name": "Yapay Zeka Beyni (AI Brain)",
        "route_pattern": "/api/custom/ai-brain",
        "code": "try {\n  const entSettings = await sql`SELECT id FROM entities WHERE slug = 'ai_settings'`;\n  if (entSettings.length === 0) throw new Error(\"Yapay Zeka Ayarları (ai_settings) tablosu bulunamadı.\");\n  \n  const recs = await sql`SELECT id FROM records WHERE entity_id = ${entSettings[0].id} ORDER BY id DESC LIMIT 1`;\n  let settingsRecord = null;\n  if (recs.length > 0) {\n    const fields = await sql`SELECT key, val_str FROM record_fields WHERE record_id = ${recs[0].id}`;\n    settingsRecord = {};\n    fields.forEach(f => { settingsRecord[f.key] = f.val_str; });\n  }\n\n  if (!settingsRecord || !settingsRecord.api_key) {\n    return { respond: true, status: 200, body: { success: false, message: \"Lütfen önce Asistan arayüzündeki çark (ayarlar) butonundan geçerli bir API Anahtarı girin ve kaydedin.\" } };\n  }\n\n  const [weatherRes, btcRes, quoteRes, exRes, newsRes] = await Promise.all([\n    fetch('https://api.open-meteo.com/v1/forecast?latitude=41.1592&longitude=27.8000&current_weather=true').catch(() => null),\n    fetch('https://api.binance.com/api/v3/ticker/price?symbol=BTCUSDT').catch(() => null),\n    fetch('https://dummyjson.com/quotes/random').catch(() => null),\n    fetch('https://api.exchangerate-api.com/v4/latest/USD').catch(() => null),\n    fetch('https://api.rss2json.com/v1/api.json?rss_url=https://www.trthaber.com/manset_articles.rss').catch(() => null)\n  ]);\n\n  const weather = weatherRes ? await weatherRes.json().catch(()=>({})) : {};\n  const btc = btcRes ? await btcRes.json().catch(()=>({})) : {};\n  const quote = quoteRes ? await quoteRes.json().catch(()=>({})) : {};\n  const ex = exRes ? await exRes.json().catch(()=>({})) : {};\n  const news = newsRes ? await newsRes.json().catch(()=>({})) : {};\n\n  const todayStr = new Date().toLocaleDateString('tr-TR');\n  \n  // Custom user prompt that overrides system prompt for strictness is no longer hardcoded here.\n  // We will now pass the prompt from settingsRecord!\n  const systemPrompt = settingsRecord.system_prompt || \"Sen profesyonel, net ve kısa konuşan bir dijital asistansın.\";\n  \n  const temp = weather?.current_weather?.temperature || 'bilinmeyen';\n  const price = btc?.price ? Math.round(btc.price) : 'bilinmeyen';\n  const usd = ex?.rates?.TRY ? ex.rates.TRY.toFixed(2) : 'bilinmeyen';\n  const eur = (ex?.rates?.TRY && ex?.rates?.EUR) ? (ex.rates.TRY / ex.rates.EUR).toFixed(2) : 'bilinmeyen';\n  const qText = quote?.quote || 'Güzel bir gün!';\n  \n  let newsText = \"Haber alınamadı.\";\n  if (news?.items && news.items.length > 0) {\n    newsText = news.items.slice(0, 4).map((n, i) => `${i+1}. ${n.title}`).join('. ');\n  }\n\n  const userPrompt = `Bugün ${todayStr}. Sana iletilen güncel veriler: Çorlu hava durumu: ${temp} derece. Kripto: Bitcoin ${price} USD. Döviz: Dolar ${usd} TL, Euro ${eur} TL. Türkiye Gündemi (Haberler): ${newsText}. Lütfen bu verileri kullanarak sistem komutundaki (system prompt) kurallara BİREBİR uyarak bana bir brifing hazırla.`;\n\n  let aiResponseText = \"\";\n\n  if (settingsRecord.ai_provider === 'openai') {\n    const aiReq = await fetch('https://api.openai.com/v1/chat/completions', {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n        'Authorization': `Bearer ${settingsRecord.api_key}`\n      },\n      body: JSON.stringify({\n        model: 'gpt-3.5-turbo',\n        messages: [\n          { role: 'system', content: systemPrompt },\n          { role: 'user', content: userPrompt }\n        ]\n      })\n    });\n    const aiRes = await aiReq.json();\n    if(aiRes.error) throw new Error(aiRes.error.message || JSON.stringify(aiRes.error));\n    aiResponseText = aiRes.choices[0].message.content;\n  } else {\n    const reqBody = {\n      model: 'gemini-3.1-flash-lite',\n      input: `${systemPrompt}\\n\\n${userPrompt}`\n    };\n    \n    const aiReq = await fetch(`https://generativelanguage.googleapis.com/v1beta/interactions`, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/json',\n        'x-goog-api-key': settingsRecord.api_key\n      },\n      body: JSON.stringify(reqBody)\n    });\n    \n    let aiRes;\n    try {\n      aiRes = await aiReq.json();\n    } catch(e) {\n      throw new Error(\"API Yanıtı JSON değil. Status: \" + aiReq.status);\n    }\n    \n    if (aiRes.error) {\n      const apiErr = aiRes.error.message || JSON.stringify(aiRes.error);\n      throw new Error(\"Gemini API Hatası: \" + apiErr);\n    }\n    if (!aiRes.steps) {\n      throw new Error(\"Gemini format hatası: steps bulunamadı. Gelen veri: \" + JSON.stringify(aiRes));\n    }\n    \n    let outputText = \"\";\n    for(const step of aiRes.steps) {\n      if(step.type === \"model_output\" && step.content) {\n        for(const c of step.content) {\n          if(c.type === \"text\") outputText += c.text;\n        }\n      }\n    }\n    if(!outputText) {\n      throw new Error(\"Gemini yanıt metni boş döndü. Yanıt: \" + JSON.stringify(aiRes));\n    }\n    aiResponseText = outputText;\n  }\n\n  const entReport = await sql`SELECT id FROM entities WHERE slug = 'ai_reports'`;\n  if (entReport.length === 0) throw new Error(\"Yapay Zeka Günlük Raporları (ai_reports) tablosu bulunamadı.\");\n  \n  const insertRec = await sql`INSERT INTO records (entity_id) VALUES (${entReport[0].id}) RETURNING id`;\n  const recId = insertRec[0].id;\n  \n  const dateKey = new Date().toISOString().split('T')[0];\n  \n  await Promise.all([\n    sql`INSERT INTO record_fields (record_id, key, val_str) VALUES (${recId}, 'date', ${dateKey})`,\n    sql`INSERT INTO record_fields (record_id, key, val_str) VALUES (${recId}, 'report_text', ${aiResponseText})`,\n    sql`INSERT INTO record_fields (record_id, key, val_bool) VALUES (${recId}, 'is_read', false)`\n  ]);\n\n  return {\n    respond: true,\n    status: 200,\n    body: { success: true, report: aiResponseText }\n  };\n\n} catch (error) {\n  let errMsg = \"Bilinmeyen Hata\";\n  if (error && error.message) errMsg = error.message;\n  else if (typeof error === 'string') errMsg = error;\n  else errMsg = JSON.stringify(error);\n  return { respond: true, status: 200, body: { success: false, message: errMsg } };\n}",
        "priority": 0,
        "active": 1,
        "is_public": 1,
        "created_at": "2026-07-03 21:52:48",
        "updated_at": "2026-07-09 07:29:14",
        "created_by": 1,
        "updated_by": 1,
        "hashtags": "[\"#assistant_ai\"]",
        "type": "http"
      }
    ]
  }
}